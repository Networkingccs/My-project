
name: Connections and actions


    # The type of runner that the job will run on
    runs-on: JAVA main
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Check if connections between peripherals are perfectly matched, only after that some conditionals may be added to this program

      # Single check if everything is in the same phase
      - Networking physical: execution of TRU-FALSE COMMAND based on conditionals
        run: Please creat a logic workflow and a syntax map before running any program
        Strings{
        <connex 1> = PC
          <connex 1> = laptop
           <connex 3> = router
           }
        IF <connex 1> = TRUE then
        <connex 2> and <connex 3>= True
        Else contador=0 
           Else IF = False

      # Runs a set of commands using the runners shell
      - IP verification: Execute a multi-scryp
        run: Be my guest on conditionals for IP ???
          echo try these connections
          echo re-try
